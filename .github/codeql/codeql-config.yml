name: "CodeQL config"

disable-default-queries: false

queries:
  - uses: security-and-quality

languages:

paths:
  - src/main/
  - python/

paths-ignore:
  - src/test/
  - '**/node_modules/'

build: |
